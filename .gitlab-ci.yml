image: docker.ccbjb.com.cn/node:latest
#image: node:latest
pages:
  cache:
    key: buildkey
    paths:
    - node_modules/

  script:
   - yarn install
   - yarn run build 
 
  artifacts:
    paths:
    - public
  only:
  - master